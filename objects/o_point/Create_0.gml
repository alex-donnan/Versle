/// @description just update yourself lmao

player = (x > room_width / 2) ? 1 : 0;

//Placement in map
letter_value = "";
	
//Animation
sub_img = check_value.missing;
y_scale = 0;
y_scale_count = floor(room_speed / 2);
color_change = false;
//alpha
alpha = (y_pos == o_data.y) ? 1 : 0;
